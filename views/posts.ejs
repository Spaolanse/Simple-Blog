<%- include('partials/header.ejs') %>

<div class="container">
    <% for(i = 0; i < locals.totalPosts; i++) { %>
        <div class="post-div">
            <h2><%= posts[i].title %></h2>
            <p><%= currentDay %></p>
            <p><%= posts[i].post %></p>
        </div>
    <% } %>
</div>

<%- include('partials/footer.ejs') %>